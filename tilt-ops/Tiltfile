# catalyst-ui — PROD/OPS spoke (scaffold)
#
# Placeholder. See ~/catalyst-devspace/workspace/catalyst-llm/tilt-ops/Tiltfile
# for the canonical pattern, and uncomment the matching include() in
# ~/catalyst-devspace/tilt-ops/Tiltfile when this spoke goes live.

load('../../../tilt-ops/common.tiltfile', 'print_prod_spoke_banner')

NAMESPACE = 'catalyst-ui'
CONTEXT = 'admin@catalyst-cluster'

allow_k8s_contexts(CONTEXT)

print_prod_spoke_banner('catalyst-ui (scaffold)', CONTEXT)
